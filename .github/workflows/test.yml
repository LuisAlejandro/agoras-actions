name: test

on:
  push:
    branches:
      - master
      - develop

jobs:
  test:
    name: test
    runs-on: ubuntu-22.04
    steps:
      - uses: actions/checkout@v2
      - name: Testing script
        uses: ./tweet-with-media
